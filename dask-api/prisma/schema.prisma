generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum MembershipRole {
  OWNER
  ADMIN
  MEMBER
  VIEWER
  CLIENT
}

enum AuditSeverity {
  INFO
  WARNING
  ERROR
}

enum CustomFieldType {
  TEXT
  LONG_TEXT
  NUMBER
  DATE
  DATETIME
  BOOLEAN
  SELECT
  MULTI_SELECT
  USER
  CHECKLIST
  PRIORITY
  STATUS
  TAG
  SCHEDULE
  WORK_ITEM_TYPE
}

enum AuthProvider {
  GOOGLE
  MICROSOFT
}

enum WorkspaceKind {
  PERSONAL
  CORPORATE
}

enum SubscriptionPlan {
  PERSONAL
  BUSINESS
}

enum SubscriptionStatus {
  INCOMPLETE
  INCOMPLETE_EXPIRED
  TRIALING
  ACTIVE
  PAST_DUE
  CANCELED
  UNPAID
  PAUSED
}

enum ConnectPaymentOrderStatus {
  DRAFT
  CHECKOUT_OPEN
  CHECKOUT_COMPLETED
  PENDING
  OVERDUE
  PAID
  FAILED
  CANCELED
  REFUNDED
  SUBSCRIPTION_ACTIVE
  SUBSCRIPTION_CANCELED
}

enum ConnectCatalogItemKind {
  PRODUCT
  SERVICE
}

enum ConnectCatalogBillingType {
  ONE_TIME
  ASSINATURA
  SUBSCRIPTION
}

enum ConnectCatalogRecurringInterval {
  DAY
  WEEK
  MONTH
  YEAR
}

enum FiscalProvider {
  FOCUS
}

enum FiscalDirection {
  OUTBOUND
  INBOUND
}

enum FiscalDocumentType {
  NFE
  NFSE
}

enum FiscalDocumentOrigin {
  MANUAL_PRODUCT
  MANUAL_SERVICE
  CATALOG_PRODUCT
  CATALOG_SERVICE
  STRIPE_PAYMENT
  STRIPE_SUBSCRIPTION
  EXTERNAL_RECEIVED_NFE
  EXTERNAL_RECEIVED_NFSE
}

enum FiscalSourceSystem {
  INTERNAL
  STRIPE
  FOCUS
  MDE
  NFSER
}

enum FiscalDocumentStatus {
  DRAFT
  READY_TO_ISSUE
  ISSUING
  AUTHORIZED
  PROCESSING
  PENDING_REVIEW
  REJECTED
  CANCELLED
  FAILED
  RECEIVED
  MANIFEST_PENDING
  MANIFESTED
  SYNCED
}

enum FiscalIssueStatus {
  NOT_STARTED
  PROCESSING
  AUTHORIZED
  REJECTED
  CANCELLED
  FAILED
}

enum FiscalPartyRole {
  EMITTER
  RECIPIENT
  TAKER
  SUPPLIER
}

enum FiscalReceivedType {
  NFE_MDE
  NFSE_NFSER
}

enum FiscalReceivedStatus {
  RECEIVED
  MANIFEST_PENDING
  MANIFESTED
  SYNCED
  FAILED
}

enum FiscalSyncType {
  RECEIVED_NFE
  RECEIVED_NFSE
  PENDING_RECONCILIATION
  OUTBOUND_STATUS
}

enum FiscalSyncTrigger {
  MANUAL
  SCHEDULED
  WEBHOOK
  RETRY
}

enum FiscalSyncStatus {
  RUNNING
  SUCCESS
  PARTIAL
  FAILED
}

enum FiscalWebhookSource {
  FOCUS
  STRIPE
}

enum FiscalWebhookStatus {
  RECEIVED
  PROCESSED
  FAILED
  DUPLICATE
}

enum FiscalCatalogItemType {
  PRODUCT
  SERVICE
}

enum FiscalDraftStatus {
  DRAFT
  READY
  ISSUED
  CANCELLED
}

enum FiscalIntegrationDirection {
  OUTBOUND
  INBOUND
}

enum FiscalIntegrationStatus {
  SUCCESS
  ERROR
}

enum LeadStatus {
  CAPTURED
  QUALIFIED
  DISTRIBUTED
  FOLLOW_UP
  NURTURING
  CONVERTED
  LOST
}

enum LeadSource {
  MANUAL
  API
  WEBHOOK
  IMPORT
  INTEGRATION
}

enum LeadQualificationStatus {
  UNQUALIFIED
  MQL
  SQL
  DISQUALIFIED
}

enum LeadDistributionStatus {
  UNASSIGNED
  ASSIGNED
  ACCEPTED
  REASSIGNED
}

enum LeadDistributionStrategy {
  MANUAL
  ROUND_ROBIN
  RULE_BASED
  TERRITORY
}

enum LeadActivityType {
  CAPTURED
  QUALIFIED
  DISTRIBUTED
  FOLLOW_UP
  NURTURE_TOUCH
  CONVERTED
  STATUS_CHANGED
  NOTE
}

enum LeadNurtureStatus {
  DRAFT
  SCHEDULED
  SENT
  RESPONDED
  SKIPPED
  FAILED
}

enum LeadConversionType {
  CUSTOMER
  OPPORTUNITY
  DEAL
  SUBSCRIPTION
}

enum LeadIntegrationSource {
  GENERIC_WEBHOOK
  ZAPIER
  MAKE
  N8N
  HUBSPOT
  RD_STATION
}

enum LeadIntegrationEventStatus {
  RECEIVED
  PROCESSED
  FAILED
  DUPLICATE
}

enum MarketingCampaignStatus {
  DRAFT
  IN_REVIEW
  APPROVED
  SCHEDULED
  ACTIVE
  PAUSED
  COMPLETED
  ARCHIVED
}

enum MarketingCampaignChannel {
  EMAIL
  NEWSLETTER
}

enum MarketingCampaignObjective {
  LEAD_NURTURE
  ONBOARDING
  REACTIVATION
  BILLING_REMINDER
  RENEWAL
  EXPANSION
  PRODUCT_UPDATE
  NEWSLETTER
  CUSTOM
}

enum MarketingAudienceType {
  STATIC
  DYNAMIC
}

enum MarketingEventType {
  CAMPAIGN_CREATED
  CAMPAIGN_UPDATED
  CAMPAIGN_REVIEW_REQUESTED
  CAMPAIGN_APPROVED
  CAMPAIGN_SCHEDULED
  CAMPAIGN_LAUNCHED
  SEND_QUEUED
  EMAIL_SENT
  EMAIL_DELIVERED
  EMAIL_OPENED
  EMAIL_CLICKED
  EMAIL_BOUNCED
  EMAIL_COMPLAINT
  EMAIL_UNSUBSCRIBED
  LEAD_SCORE_CHANGED
  AUTOMATION_ENROLLED
  AUTOMATION_EXITED
  OPPORTUNITY_INFLUENCED
  CUSTOMER_CONVERTED
  BILLING_SIGNAL_TRIGGERED
  AI_GENERATED
  AI_IMPROVED
  TASK_CREATED
  DOCUMENT_LINKED
}

enum MarketingConsentStatus {
  OPT_IN
  OPT_OUT
  UNSUBSCRIBED
  UNKNOWN
}

enum MarketingMessageKind {
  MARKETING
  TRANSACTIONAL
}

enum MarketingSendStatus {
  QUEUED
  SENT
  DELIVERED
  OPENED
  CLICKED
  BOUNCED
  COMPLAINED
  UNSUBSCRIBED
  FAILED
  SKIPPED
}

enum MarketingAutomationStatus {
  DRAFT
  ACTIVE
  PAUSED
  ARCHIVED
}

enum MarketingAutomationStepKind {
  TRIGGER
  CONDITION
  DELAY
  ACTION
  BRANCH
  EXIT
}

enum MarketingAutomationEnrollmentStatus {
  ACTIVE
  COMPLETED
  EXITED
  FAILED
}

enum MarketingAttributionType {
  FIRST_TOUCH
  LAST_TOUCH
  MULTI_TOUCH
}

enum MarketingAttributionEntityType {
  LEAD
  OPPORTUNITY
  CUSTOMER
  SUBSCRIPTION
  INVOICE
}

model User {
  id                       String                    @id @default(uuid())
  email                    String                    @unique
  name                     String
  emailVerified            Boolean                   @default(false)
  isPlatformAdmin          Boolean                   @default(false)
  passwordHash             String?
  // 0 = legacy plain-bcrypt (pre-migration), 1 = HMAC-SHA256+bcrypt with pepper
  passwordHashVersion      Int                       @default(0)
  loginFailureCount        Int                       @default(0)
  lockedUntil              DateTime?
  preferences              Json?
  // Billing / Stripe
  stripeCustomerId         String?                   @unique
  subscriptionPlan         SubscriptionPlan?
  subscriptionStatus       SubscriptionStatus?
  subscriptionId           String?
  currentPeriodEnd         DateTime?
  hasActiveSubscription    Boolean                   @default(false)
  createdAt                DateTime                  @default(now())
  updatedAt                DateTime                  @updatedAt
  organizations            OrganizationMembership[]
  teams                    TeamMembership[]
  workspaces               WorkspaceMembership[]
  createdItems             Item[]                    @relation("ItemCreatedBy")
  assignedItems            Item[]                    @relation("ItemAssignee")
  updatedItems             Item[]                    @relation("ItemUpdatedBy")
  comments                 ItemComment[]
  updatedCustomFieldValues CustomFieldValue[]        @relation("CustomFieldValueUpdatedBy")
  updatedViewPlacements    WorkItemViewPlacement[]   @relation("WorkItemViewPlacementUpdatedBy")
  customerWorkspaceLinks    WorkspaceCustomerUser[]
  refreshTokens            RefreshToken[]
  resetTokens              PasswordResetToken[]
  verificationTokens       EmailVerificationToken[]
  externalIdentities       ExternalIdentity[]
  subscriptions            Subscription[]
  sentWorkspaceInvites     WorkspaceInvite[]        @relation("WorkspaceInviteSentBy")
  acceptedWorkspaceInvites WorkspaceInvite[]        @relation("WorkspaceInviteAcceptedBy")
  createdAutomationWorkflows AutomationWorkflow[]   @relation("AutomationWorkflowCreatedBy")
  publishedAutomationWorkflowVersions AutomationWorkflowVersion[] @relation("AutomationWorkflowVersionPublishedBy")
  createdCommunicationTemplates CommunicationTemplate[] @relation("CommunicationTemplateCreatedBy")
  publishedCommunicationTemplateVersions CommunicationTemplateVersion[] @relation("CommunicationTemplateVersionPublishedBy")
  assignedCommunicationConversations CommunicationConversation[] @relation("CommunicationConversationAssignedTo")
  ownedLeads               Lead[]                  @relation("LeadOwner")
  leadActivities           LeadActivity[]          @relation("LeadActivityActor")
  leadAssignments          LeadAssignment[]        @relation("LeadAssignmentActor")
  leadConversions          LeadConversion[]        @relation("LeadConversionActor")
  leadNurtureTouches       LeadNurtureTouch[]      @relation("LeadNurtureActor")
}

model Subscription {
  id                       String             @id @default(uuid())
  userId                   String
  stripeCustomerId         String
  stripeSubscriptionId     String             @unique
  stripeCheckoutSessionId  String?
  planCode                 SubscriptionPlan
  status                   SubscriptionStatus
  currency                 String             @default("brl")
  amount                   Int                // in cents
  interval                 String             @default("month")
  currentPeriodStart       DateTime?
  currentPeriodEnd         DateTime?
  cancelAtPeriodEnd        Boolean            @default(false)
  lastWebhookEvent         String?
  createdAt                DateTime           @default(now())
  updatedAt                DateTime           @updatedAt
  user                     User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([stripeCustomerId])
}

model ExternalIdentity {
  id               String       @id @default(uuid())
  userId           String
  provider         AuthProvider
  providerSubject  String
  providerTenantId String?
  emailAtProvider  String?
  emailVerified    Boolean?
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerSubject, providerTenantId])
  @@index([userId])
}

model RefreshToken {
  id        String    @id @default(uuid())
  userId    String
  tokenHash String    @unique
  // familyId links tokens issued from the same login session;
  // reuse of a revoked token triggers revocation of the entire family
  familyId  String
  expiresAt DateTime
  revokedAt DateTime?
  createdAt DateTime  @default(now())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([familyId])
}

model PasswordResetToken {
  id        String    @id @default(uuid())
  userId    String
  tokenHash String    @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model EmailVerificationToken {
  id        String    @id @default(uuid())
  userId    String
  tokenHash String    @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model Organization {
  id              String                    @id @default(uuid())
  name            String
  slug            String                    @unique
  settings        Json?
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  memberships     OrganizationMembership[]
  teams           Team[]
  workspaces      Workspace[]
}

model OrganizationMembership {
  id              String                    @id @default(uuid())
  organizationId  String
  userId          String
  role            MembershipRole            @default(MEMBER)
  permissions     Json?
  createdAt       DateTime                  @default(now())
  organization    Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  user            User                      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([organizationId, userId])
}

model Team {
  id              String                    @id @default(uuid())
  organizationId  String
  name            String
  metadata        Json?
  createdAt       DateTime                  @default(now())
  organization    Organization              @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  memberships     TeamMembership[]
}

model TeamMembership {
  id              String                    @id @default(uuid())
  teamId          String
  userId          String
  role            MembershipRole            @default(MEMBER)
  permissions     Json?
  createdAt       DateTime                  @default(now())
  team            Team                      @relation(fields: [teamId], references: [id], onDelete: Cascade)
  user            User                      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([teamId, userId])
}

model Workspace {
  id                     String                    @id @default(uuid())
  organizationId         String?
  kind                   WorkspaceKind             @default(CORPORATE)
  name                   String
  key                    String
  config                 Json?
  createdAt              DateTime                  @default(now())
  updatedAt              DateTime                  @updatedAt
  organization           Organization?             @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  memberships            WorkspaceMembership[]
  boards                 Board[]
  templates              BoardTemplate[]
  itemTypes              WorkItemType[]
  workflowStates         WorkflowState[]
  boardColumns           BoardColumn[]
  columnStateMappings    ColumnStateMapping[]
  tagDefinitions         TagDefinition[]
  customFieldDefinitions CustomFieldDefinition[]
  fieldBindings          WorkItemFieldBinding[]
  preferences            WorkspacePreferences?
  workItems              Item[]
  automationViews        AutomationView[]
  automationViewColumns  AutomationViewColumn[]
  itemViewPlacements     WorkItemViewPlacement[]
  automationWorkflows    AutomationWorkflow[]
  automationWorkflowVersions AutomationWorkflowVersion[]
  automationRuns         AutomationRun[]
  automationStepRuns     AutomationStepRun[]
  automationScheduledSteps AutomationScheduledStep[]
  automationRunEvents    AutomationRunEvent[]
  automationSideEffects  AutomationSideEffect[]
  automationApprovalRequests AutomationApprovalRequest[]
  communicationTemplates CommunicationTemplate[]
  communicationTemplateVersions CommunicationTemplateVersion[]
  contactConsents        ContactConsent[]
  communicationSuppressions CommunicationSuppression[]
  communicationUnsubscribeTokens CommunicationUnsubscribeToken[]
  communicationProviderEvents CommunicationProviderEvent[]
  communicationContacts CommunicationContact[]
  communicationContactChannels CommunicationContactChannel[]
  communicationConversations CommunicationConversation[]
  communicationInteractions CommunicationInteraction[]
  communicationConversationWindows CommunicationConversationWindow[]
  aiAgents               AIAgent[]
  aiAgentRuns            AIAgentRun[]
  invites                WorkspaceInvite[]
  documents              WorkspaceDocument[]
  workItemDocumentLinks  WorkItemDocumentLink[]
  connectPaymentOrders   ConnectPaymentOrder[]
  connectCatalogItems    ConnectCatalogItem[]
  fiscalCompanyConfigs   FiscalCompanyConfig[]
  fiscalDocuments        FiscalDocument[]
  fiscalEmissionDrafts   FiscalEmissionDraft[]
  fiscalReceivedDocuments FiscalReceivedDocument[]
  fiscalSyncRuns         FiscalSyncRun[]
  fiscalWebhookEvents    FiscalWebhookEvent[]
  fiscalIntegrationLogs  FiscalIntegrationLog[]
  fiscalCatalogProfiles  FiscalCatalogProfile[]
  fiscalOperationTemplates FiscalOperationTemplate[]
  customers              Customer[]
  customerUserLinks      WorkspaceCustomerUser[]
  leads                  Lead[]
  leadActivities         LeadActivity[]
  leadAssignments        LeadAssignment[]
  leadNurtureTouches     LeadNurtureTouch[]
  leadConversions        LeadConversion[]
  leadIntegrationEvents  LeadIntegrationEvent[]
  marketingCampaigns     MarketingCampaign[]
  marketingCampaignVariants MarketingCampaignVariant[]
  marketingAudienceSegments MarketingAudienceSegment[]
  marketingContactPreferences MarketingContactPreference[]
  marketingEmailTemplates MarketingEmailTemplate[]
  marketingAutomationFlows MarketingAutomationFlow[]
  marketingAutomationSteps MarketingAutomationStep[]
  marketingAutomationEnrollments MarketingAutomationEnrollment[]
  marketingEvents        MarketingEvent[]
  marketingAttributions  MarketingAttribution[]
  marketingSenderProfiles MarketingSenderProfile[]
  marketingConsentRecords MarketingConsentRecord[]
  marketingLeadScoreEvents MarketingLeadScoreEvent[]
  marketingContentAssets MarketingContentAsset[]
  marketingCampaignSends MarketingCampaignSend[]

  @@unique([organizationId, key])
}

model Customer {
  id          String    @id @default(uuid())
  workspaceId String
  name        String
  tradeName   String?
  legalName   String?
  document    String?
  stateRegistration String?
  municipalRegistration String?
  taxRegime   String?
  email       String?
  phone       String?
  website     String?
  logoUrl     String?
  address     Json?
  status      String    @default("prospect")
  notes       String?
  createdBy   String?
  updatedBy   String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  leads       Lead[]
  connectPaymentOrders ConnectPaymentOrder[]
  userLinks   WorkspaceCustomerUser[]

  @@index([workspaceId, status, updatedAt])
  @@index([workspaceId, name])
  @@index([workspaceId, email])
  @@index([workspaceId, phone])
  @@index([workspaceId, document])
}

model WorkspaceCustomerUser {
  id          String    @id @default(uuid())
  workspaceId String
  customerId  String
  userId      String
  createdBy   String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  customer    Customer  @relation(fields: [customerId], references: [id], onDelete: Cascade)
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([workspaceId, customerId, userId])
  @@index([workspaceId, userId])
  @@index([workspaceId, customerId])
}

model Lead {
  id                   String                    @id @default(uuid())
  workspaceId          String
  customerId           String?
  externalSource       LeadIntegrationSource?
  externalId           String?
  captureSource        LeadSource                @default(MANUAL)
  status               LeadStatus                @default(CAPTURED)
  qualificationStatus  LeadQualificationStatus   @default(UNQUALIFIED)
  distributionStatus   LeadDistributionStatus    @default(UNASSIGNED)
  score                Int                       @default(0)
  temperature          String?
  firstName            String?
  lastName             String?
  fullName             String?
  email                String?
  phone                String?
  companyName          String?
  jobTitle             String?
  website              String?
  city                 String?
  state                String?
  country              String?
  interest             String?
  notes                String?
  tags                 Json?
  ownerUserId          String?
  estimatedValue       Decimal?                  @db.Decimal(18, 2)
  currency             String                    @default("BRL")
  qualifiedAt          DateTime?
  distributedAt        DateTime?
  lastContactAt        DateTime?
  nextFollowUpAt       DateTime?
  nurturingStartedAt   DateTime?
  convertedAt          DateTime?
  lostAt               DateTime?
  metadata             Json?
  createdByUserId      String?
  updatedByUserId      String?
  createdAt            DateTime                  @default(now())
  updatedAt            DateTime                  @updatedAt
  workspace            Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  customer             Customer?                 @relation(fields: [customerId], references: [id], onDelete: SetNull)
  owner                User?                     @relation("LeadOwner", fields: [ownerUserId], references: [id], onDelete: SetNull)
  activities           LeadActivity[]
  assignments          LeadAssignment[]
  nurtureTouches       LeadNurtureTouch[]
  conversion           LeadConversion?
  integrationEvents    LeadIntegrationEvent[]
  marketingPreferences MarketingContactPreference[]
  marketingEvents      MarketingEvent[]
  marketingAttributions MarketingAttribution[]
  marketingLeadScoreEvents MarketingLeadScoreEvent[]
  marketingCampaignSends MarketingCampaignSend[]
  marketingAutomationEnrollments MarketingAutomationEnrollment[]
  marketingConsentRecords MarketingConsentRecord[]

  @@unique([workspaceId, externalSource, externalId])
  @@index([workspaceId, status, createdAt])
  @@index([workspaceId, customerId])
  @@index([workspaceId, qualificationStatus, distributionStatus, createdAt])
  @@index([workspaceId, ownerUserId, nextFollowUpAt])
  @@index([workspaceId, email, phone])
}

model LeadActivity {
  id            String            @id @default(uuid())
  workspaceId   String
  leadId        String
  type          LeadActivityType
  title         String
  description   String?
  payload       Json?
  actorUserId   String?
  occurredAt    DateTime          @default(now())
  createdAt     DateTime          @default(now())
  workspace     Workspace         @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead          Lead              @relation(fields: [leadId], references: [id], onDelete: Cascade)
  actor         User?             @relation("LeadActivityActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([workspaceId, leadId, occurredAt])
  @@index([workspaceId, type, occurredAt])
}

model LeadAssignment {
  id                  String                    @id @default(uuid())
  workspaceId         String
  leadId              String
  fromUserId          String?
  toUserId            String?
  strategy            LeadDistributionStrategy  @default(MANUAL)
  reason              String?
  assignedByUserId    String?
  createdAt           DateTime                  @default(now())
  workspace           Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead                Lead                      @relation(fields: [leadId], references: [id], onDelete: Cascade)
  actor               User?                     @relation("LeadAssignmentActor", fields: [assignedByUserId], references: [id], onDelete: SetNull)

  @@index([workspaceId, leadId, createdAt])
  @@index([workspaceId, toUserId, createdAt])
}

model LeadNurtureTouch {
  id                 String             @id @default(uuid())
  workspaceId        String
  leadId             String
  status             LeadNurtureStatus  @default(DRAFT)
  channel            String
  templateKey        String?
  subject            String?
  message            String?
  scheduledAt        DateTime?
  sentAt             DateTime?
  respondedAt        DateTime?
  providerMessageId  String?
  errorMessage       String?
  metadata           Json?
  createdByUserId    String?
  createdAt          DateTime           @default(now())
  updatedAt          DateTime           @updatedAt
  workspace          Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead               Lead               @relation(fields: [leadId], references: [id], onDelete: Cascade)
  actor              User?              @relation("LeadNurtureActor", fields: [createdByUserId], references: [id], onDelete: SetNull)

  @@index([workspaceId, leadId, status, createdAt])
  @@index([workspaceId, scheduledAt])
}

model LeadConversion {
  id                 String              @id @default(uuid())
  workspaceId        String
  leadId             String              @unique
  conversionType     LeadConversionType  @default(CUSTOMER)
  conversionRef      String
  amount             Decimal?            @db.Decimal(18, 2)
  currency           String              @default("BRL")
  notes              String?
  convertedByUserId  String?
  convertedAt        DateTime            @default(now())
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  workspace          Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead               Lead                @relation(fields: [leadId], references: [id], onDelete: Cascade)
  actor              User?               @relation("LeadConversionActor", fields: [convertedByUserId], references: [id], onDelete: SetNull)

  @@index([workspaceId, conversionType, convertedAt])
}

model LeadIntegrationEvent {
  id               String                      @id @default(uuid())
  workspaceId      String?
  leadId           String?
  source           LeadIntegrationSource       @default(GENERIC_WEBHOOK)
  eventType        String
  providerEventId  String?
  idempotencyKey   String                      @unique
  status           LeadIntegrationEventStatus  @default(RECEIVED)
  headers          Json?
  payload          Json
  attempts         Int                         @default(0)
  receivedAt       DateTime                    @default(now())
  processedAt      DateTime?
  lastError        String?
  createdAt        DateTime                    @default(now())
  updatedAt        DateTime                    @updatedAt
  workspace        Workspace?                  @relation(fields: [workspaceId], references: [id], onDelete: SetNull)
  lead             Lead?                       @relation(fields: [leadId], references: [id], onDelete: SetNull)

  @@index([workspaceId, status, createdAt])
  @@index([source, eventType, createdAt])
}

model MarketingAudienceSegment {
  id                String               @id @default(uuid())
  workspaceId       String
  name              String
  description       String?
  kind              MarketingAudienceType @default(DYNAMIC)
  filters           Json
  isActive          Boolean              @default(true)
  isSystem          Boolean              @default(false)
  lastEvaluatedAt   DateTime?
  estimatedContacts Int?
  createdByUserId   String?
  updatedByUserId   String?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt
  workspace         Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaigns         MarketingCampaign[]
  events            MarketingEvent[]

  @@unique([workspaceId, name])
  @@index([workspaceId, isActive, updatedAt])
}

model MarketingEmailTemplate {
  id                   String                    @id @default(uuid())
  workspaceId          String
  name                 String
  slug                 String
  category             String?
  objective            MarketingCampaignObjective?
  funnelStage          String?
  subject              String
  bodyMarkdown         String
  bodyHtml             String?
  blocks               Json?
  performanceSnapshot  Json?
  isSystem             Boolean                   @default(false)
  isArchived           Boolean                   @default(false)
  createdByUserId      String?
  updatedByUserId      String?
  createdAt            DateTime                  @default(now())
  updatedAt            DateTime                  @updatedAt
  workspace            Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaigns            MarketingCampaign[]
  variants             MarketingCampaignVariant[]
  assets               MarketingContentAsset[]

  @@unique([workspaceId, slug])
  @@index([workspaceId, isArchived, updatedAt])
}

model MarketingSenderProfile {
  id             String                   @id @default(uuid())
  workspaceId    String
  name           String
  fromName       String
  fromEmail      String
  replyToEmail   String?
  providerKey    String                   @default("default")
  domainStatus   String?
  isDefault      Boolean                  @default(false)
  isVerified     Boolean                  @default(false)
  config         Json?
  createdByUserId String?
  updatedByUserId String?
  createdAt      DateTime                 @default(now())
  updatedAt      DateTime                 @updatedAt
  workspace      Workspace                @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaigns      MarketingCampaign[]
  sends          MarketingCampaignSend[]

  @@unique([workspaceId, fromEmail])
  @@index([workspaceId, isDefault])
}

model MarketingCampaign {
  id                 String                    @id @default(uuid())
  workspaceId        String
  name               String
  description        String?
  objective          MarketingCampaignObjective
  channel            MarketingCampaignChannel  @default(EMAIL)
  status             MarketingCampaignStatus   @default(DRAFT)
  hypothesis         String?
  persona            String?
  icp                String?
  offer              String?
  productRef         String?
  billingContext     String?
  segmentId          String?
  templateId         String?
  senderProfileId    String?
  abTestEnabled      Boolean                   @default(false)
  abTestConfig       Json?
  scheduledAt        DateTime?
  launchedAt         DateTime?
  completedAt        DateTime?
  archivedAt         DateTime?
  approvalRequestedAt DateTime?
  approvedAt         DateTime?
  approvedByUserId   String?
  aiGenerated        Boolean                   @default(false)
  aiContext          Json?
  createdByUserId    String?
  updatedByUserId    String?
  createdAt          DateTime                  @default(now())
  updatedAt          DateTime                  @updatedAt
  workspace          Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  segment            MarketingAudienceSegment? @relation(fields: [segmentId], references: [id], onDelete: SetNull)
  template           MarketingEmailTemplate?   @relation(fields: [templateId], references: [id], onDelete: SetNull)
  senderProfile      MarketingSenderProfile?   @relation(fields: [senderProfileId], references: [id], onDelete: SetNull)
  variants           MarketingCampaignVariant[]
  events             MarketingEvent[]
  attributions       MarketingAttribution[]
  sends              MarketingCampaignSend[]
  contentAssets      MarketingContentAsset[]
  automationEnrollments MarketingAutomationEnrollment[]
  leadScoreEvents    MarketingLeadScoreEvent[]

  @@index([workspaceId, status, scheduledAt])
  @@index([workspaceId, objective, createdAt])
  @@index([workspaceId, segmentId, updatedAt])
}

model MarketingCampaignVariant {
  id              String              @id @default(uuid())
  workspaceId     String
  campaignId      String
  templateId      String?
  name            String
  isControl       Boolean             @default(false)
  weight          Int                 @default(100)
  subject         String
  preheader       String?
  bodyMarkdown    String
  bodyHtml        String?
  variables       Json?
  aiGenerated     Boolean             @default(false)
  aiMetadata      Json?
  createdByUserId String?
  updatedByUserId String?
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt
  workspace       Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaign        MarketingCampaign   @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  template        MarketingEmailTemplate? @relation(fields: [templateId], references: [id], onDelete: SetNull)
  sends           MarketingCampaignSend[]
  events          MarketingEvent[]

  @@index([campaignId, weight])
  @@index([workspaceId, campaignId, createdAt])
}

model MarketingContactPreference {
  id               String                 @id @default(uuid())
  workspaceId      String
  leadId           String?
  email            String
  messageKind      MarketingMessageKind   @default(MARKETING)
  consentStatus    MarketingConsentStatus @default(UNKNOWN)
  consentSource    String?
  legalBasis       String?
  allowEmail       Boolean                @default(true)
  allowNewsletter  Boolean                @default(true)
  allowBilling     Boolean                @default(true)
  unsubscribeAt    DateTime?
  preferences      Json?
  lastEngagementAt DateTime?
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  workspace        Workspace              @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead             Lead?                  @relation(fields: [leadId], references: [id], onDelete: SetNull)

  @@unique([workspaceId, email, messageKind])
  @@unique([workspaceId, leadId, messageKind])
  @@index([workspaceId, consentStatus, updatedAt])
  @@index([workspaceId, leadId])
}

model MarketingConsentRecord {
  id            String                 @id @default(uuid())
  workspaceId   String
  leadId        String?
  email         String
  messageKind   MarketingMessageKind   @default(MARKETING)
  status        MarketingConsentStatus
  source        String
  reason        String?
  legalBasis    String?
  evidence      Json?
  recordedAt    DateTime               @default(now())
  actorUserId   String?
  ipAddress     String?
  userAgent     String?
  createdAt     DateTime               @default(now())
  workspace     Workspace              @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead          Lead?                  @relation(fields: [leadId], references: [id], onDelete: SetNull)

  @@index([workspaceId, email, recordedAt])
  @@index([workspaceId, leadId, recordedAt])
}

model MarketingCampaignSend {
  id              String               @id @default(uuid())
  workspaceId     String
  campaignId      String
  variantId       String
  leadId          String?
  senderProfileId String?
  contactEmail    String
  status          MarketingSendStatus  @default(QUEUED)
  idempotencyKey  String               @unique
  providerKey     String?
  providerMessageId String?
  scheduledAt     DateTime?
  sentAt          DateTime?
  deliveredAt     DateTime?
  openedAt        DateTime?
  clickedAt       DateTime?
  bouncedAt       DateTime?
  complainedAt    DateTime?
  unsubscribedAt  DateTime?
  failedAt        DateTime?
  errorMessage    String?
  metadata        Json?
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  workspace       Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaign        MarketingCampaign    @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  variant         MarketingCampaignVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)
  lead            Lead?                @relation(fields: [leadId], references: [id], onDelete: SetNull)
  senderProfile   MarketingSenderProfile? @relation(fields: [senderProfileId], references: [id], onDelete: SetNull)
  events          MarketingEvent[]

  @@index([workspaceId, campaignId, status, createdAt])
  @@index([workspaceId, leadId, createdAt])
  @@index([workspaceId, providerMessageId])
}

model MarketingAutomationFlow {
  id               String                    @id @default(uuid())
  workspaceId      String
  name             String
  description      String?
  status           MarketingAutomationStatus @default(DRAFT)
  triggerDefinition Json
  entryCriteria    Json?
  exitCriteria     Json?
  createdByUserId  String?
  updatedByUserId  String?
  createdAt        DateTime                  @default(now())
  updatedAt        DateTime                  @updatedAt
  workspace        Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  steps            MarketingAutomationStep[]
  enrollments      MarketingAutomationEnrollment[]
  events           MarketingEvent[]

  @@unique([workspaceId, name])
  @@index([workspaceId, status, updatedAt])
}

model MarketingAutomationStep {
  id          String                    @id @default(uuid())
  workspaceId String
  flowId      String
  key         String
  name        String
  kind        MarketingAutomationStepKind
  position    Int                       @default(0)
  config      Json?
  nextStepId  String?
  createdAt   DateTime                  @default(now())
  updatedAt   DateTime                  @updatedAt
  workspace   Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  flow        MarketingAutomationFlow   @relation(fields: [flowId], references: [id], onDelete: Cascade)

  @@unique([flowId, key])
  @@index([workspaceId, flowId, position])
}

model MarketingAutomationEnrollment {
  id           String                              @id @default(uuid())
  workspaceId  String
  flowId       String
  leadId       String?
  campaignId   String?
  status       MarketingAutomationEnrollmentStatus @default(ACTIVE)
  entryEventId String?
  exitReason   String?
  context      Json?
  startedAt    DateTime                            @default(now())
  nextRunAt    DateTime?
  completedAt  DateTime?
  createdAt    DateTime                            @default(now())
  updatedAt    DateTime                            @updatedAt
  workspace    Workspace                           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  flow         MarketingAutomationFlow             @relation(fields: [flowId], references: [id], onDelete: Cascade)
  lead         Lead?                               @relation(fields: [leadId], references: [id], onDelete: SetNull)
  campaign     MarketingCampaign?                  @relation(fields: [campaignId], references: [id], onDelete: SetNull)

  @@index([workspaceId, flowId, status, startedAt])
  @@index([workspaceId, leadId, startedAt])
}

model MarketingEvent {
  id               String              @id @default(uuid())
  workspaceId      String
  type             MarketingEventType
  campaignId       String?
  variantId        String?
  segmentId        String?
  leadId           String?
  sendId           String?
  automationFlowId String?
  documentId       String?
  itemId           String?
  billingRef       String?
  externalEventId  String?
  headline         String?
  description      String?
  payload          Json?
  occurredAt       DateTime            @default(now())
  seenAt           DateTime?
  dismissedAt      DateTime?
  createdAt        DateTime            @default(now())
  workspace        Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaign         MarketingCampaign?  @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  variant          MarketingCampaignVariant? @relation(fields: [variantId], references: [id], onDelete: SetNull)
  segment          MarketingAudienceSegment? @relation(fields: [segmentId], references: [id], onDelete: SetNull)
  lead             Lead?               @relation(fields: [leadId], references: [id], onDelete: SetNull)
  send             MarketingCampaignSend? @relation(fields: [sendId], references: [id], onDelete: SetNull)
  automationFlow   MarketingAutomationFlow? @relation(fields: [automationFlowId], references: [id], onDelete: SetNull)
  leadScoreEvents  MarketingLeadScoreEvent[]

  @@unique([workspaceId, externalEventId])
  @@index([workspaceId, occurredAt])
  @@index([workspaceId, leadId, occurredAt])
  @@index([workspaceId, campaignId, occurredAt])
  @@index([workspaceId, type, occurredAt])
}

model MarketingLeadScoreEvent {
  id             String             @id @default(uuid())
  workspaceId    String
  leadId         String
  campaignId     String?
  eventId        String?
  delta          Int
  previousScore  Int?
  nextScore      Int?
  reason         String
  isAutomated    Boolean            @default(true)
  explanation    Json?
  createdByUserId String?
  createdAt      DateTime           @default(now())
  workspace      Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  lead           Lead               @relation(fields: [leadId], references: [id], onDelete: Cascade)
  campaign       MarketingCampaign? @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  event          MarketingEvent?    @relation(fields: [eventId], references: [id], onDelete: SetNull)

  @@index([workspaceId, leadId, createdAt])
  @@index([workspaceId, campaignId, createdAt])
}

model MarketingAttribution {
  id               String                         @id @default(uuid())
  workspaceId      String
  campaignId       String
  leadId           String?
  attributionType  MarketingAttributionType
  entityType       MarketingAttributionEntityType
  entityRef        String
  influenceScore   Decimal?                       @db.Decimal(6, 4)
  revenueInfluenced Decimal?                      @db.Decimal(18, 2)
  currency         String                         @default("BRL")
  happenedAt       DateTime                       @default(now())
  metadata         Json?
  createdAt        DateTime                       @default(now())
  workspace        Workspace                      @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaign         MarketingCampaign              @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  lead             Lead?                          @relation(fields: [leadId], references: [id], onDelete: SetNull)

  @@index([workspaceId, entityType, entityRef])
  @@index([workspaceId, campaignId, happenedAt])
  @@index([workspaceId, leadId, happenedAt])
}

model MarketingContentAsset {
  id             String                 @id @default(uuid())
  workspaceId    String
  campaignId     String?
  templateId     String?
  documentId     String?
  title          String
  assetType      String
  content        String
  metadata       Json?
  createdByUserId String?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  workspace      Workspace              @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  campaign       MarketingCampaign?     @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  template       MarketingEmailTemplate? @relation(fields: [templateId], references: [id], onDelete: SetNull)
  document       WorkspaceDocument?     @relation(fields: [documentId], references: [id], onDelete: SetNull)

  @@index([workspaceId, campaignId, createdAt])
  @@index([workspaceId, templateId, createdAt])
  @@index([workspaceId, documentId, createdAt])
}

model WorkspaceDocument {
  id          String    @id @default(uuid())
  workspaceId String
  title       String
  content     String
  kind        String    @default("wiki")
  linkedEntityType String?
  linkedEntityId   String?
  tags        String[]  @default([])
  metadata    Json?
  position    Int       @default(0)
  createdBy   String
  updatedBy   String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  itemLinks   WorkItemDocumentLink[]
  marketingAssets MarketingContentAsset[]

  @@index([workspaceId, updatedAt])
  @@index([workspaceId, position])
  @@index([workspaceId, kind, updatedAt])
  @@index([workspaceId, linkedEntityType, linkedEntityId])
}

model ConnectPaymentOrder {
  id                    String                    @id @default(uuid())
  workspaceId           String
  customerId            String?
  createdByUserId       String
  stripeConnectAccountId String
  stripeCheckoutSessionId String?                 @unique
  stripePaymentIntentId String?                   @unique
  amount                Int
  currency              String                    @default("brl")
  description           String
  customerName          String?
  customerEmail         String?
  customerDocument      String?
  customerPhone         String?
  customerAddress       Json?
  applicationFeeAmount  Int
  status                ConnectPaymentOrderStatus @default(DRAFT)
  statusReason          String?
  metadata              Json?
  checkoutUrl           String?
  lastWebhookEvent      String?
  paidAt                DateTime?
  failedAt              DateTime?
  canceledAt            DateTime?
  refundedAt            DateTime?
  createdAt             DateTime                  @default(now())
  updatedAt             DateTime                  @updatedAt
  workspace             Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  customer              Customer?                 @relation(fields: [customerId], references: [id], onDelete: SetNull)

  @@index([workspaceId, createdAt])
  @@index([workspaceId, customerId, createdAt])
  @@index([status, createdAt])
  @@index([stripeConnectAccountId, createdAt])
}

model ConnectCatalogItem {
  id          String                 @id @default(uuid())
  workspaceId String
  createdByUserId String
  kind        ConnectCatalogItemKind @default(SERVICE)
  billingType ConnectCatalogBillingType @default(ONE_TIME)
  recurringInterval ConnectCatalogRecurringInterval?
  recurringIntervalCount Int?
  name        String
  description String?
  amount      Int
  currency    String                 @default("brl")
  stripeConnectAccountId String?
  stripeProductId String?            @unique
  stripePriceId String?              @unique
  isActive    Boolean                @default(true)
  metadata    Json?
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt
  workspace   Workspace              @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([workspaceId, isActive, createdAt])
  @@index([workspaceId, stripeConnectAccountId, createdAt])
}

model FiscalCompanyConfig {
  id                      String         @id @default(uuid())
  workspaceId             String
  workspaceBusinessId     String?
  provider                FiscalProvider @default(FOCUS)
  displayName             String
  legalName               String
  cnpj                    String
  stateRegistration       String?
  municipalRegistration   String?
  taxRegime               String?
  focusToken              String
  focusEnvironment        String         @default("homologacao")
  focusCompanyReference   String?
  focusWebhookSecret      String?
  emitAutomatically       Boolean        @default(false)
  stripePolicy            String         @default("assisted_one_click")
  defaultSerie            String?
  defaultNatureOperation  String?
  fallbackRules           Json?
  syncConfig              Json?
  metadata                Json?
  createdByUserId         String?
  createdAt               DateTime       @default(now())
  updatedAt               DateTime       @updatedAt
  workspace               Workspace      @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  documents               FiscalDocument[]
  drafts                  FiscalEmissionDraft[]
  receivedDocuments       FiscalReceivedDocument[]
  syncRuns                FiscalSyncRun[]
  integrationLogs         FiscalIntegrationLog[]

  @@unique([workspaceId, cnpj])
  @@index([workspaceId, workspaceBusinessId, createdAt])
}

model FiscalDocument {
  id                      String                @id @default(uuid())
  workspaceId             String
  workspaceBusinessId     String?
  companyConfigId         String?
  internalReference       String
  provider                FiscalProvider        @default(FOCUS)
  direction               FiscalDirection
  documentType            FiscalDocumentType
  origin                  FiscalDocumentOrigin
  sourceSystem            FiscalSourceSystem    @default(INTERNAL)
  status                  FiscalDocumentStatus  @default(DRAFT)
  issueStatus             FiscalIssueStatus     @default(NOT_STARTED)
  focusStatus             String?
  operationStatus         String?
  customerId              String?
  supplierId              String?
  saleId                  String?
  stripeSessionId         String?
  stripePaymentIntentId   String?
  stripeChargeId          String?
  stripeAccountId         String?
  focusReference          String?
  focusDocumentId         String?
  number                  String?
  series                  String?
  amountSubtotal          Decimal?              @db.Decimal(18, 4)
  amountDiscount          Decimal?              @db.Decimal(18, 4)
  amountTotal             Decimal?              @db.Decimal(18, 4)
  currency                String                @default("BRL")
  issuedAt                DateTime?
  authorizedAt            DateTime?
  cancelledAt             DateTime?
  xmlUrl                  String?
  pdfUrl                  String?
  xmlStorageRef           String?
  pdfStorageRef           String?
  requestPayloadSnapshot  Json?
  responsePayloadSnapshot Json?
  providerPayloadRaw      Json?
  metadata                Json?
  lastSyncAt              DateTime?
  lastError               String?
  createdByUserId         String?
  updatedByUserId         String?
  createdAt               DateTime              @default(now())
  updatedAt               DateTime              @updatedAt
  workspace               Workspace             @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyConfig           FiscalCompanyConfig?  @relation(fields: [companyConfigId], references: [id], onDelete: SetNull)
  items                   FiscalDocumentItem[]
  parties                 FiscalParty[]
  integrationLogs         FiscalIntegrationLog[]

  @@unique([workspaceId, internalReference])
  @@index([workspaceId, workspaceBusinessId, direction, documentType, createdAt])
  @@index([workspaceId, status, issueStatus, createdAt])
  @@index([workspaceId, saleId, stripeSessionId])
  @@index([focusReference, focusDocumentId])
}

model FiscalDocumentItem {
  id                    String         @id @default(uuid())
  documentId            String
  itemType              FiscalCatalogItemType
  sourceType            String?
  catalogProfileId      String?
  sku                   String?
  name                  String
  descriptionCommercial String?
  descriptionFiscal     String?
  quantity              Decimal        @db.Decimal(18, 4)
  unit                  String?
  unitPrice             Decimal        @db.Decimal(18, 4)
  discountAmount        Decimal?       @db.Decimal(18, 4)
  totalAmount           Decimal        @db.Decimal(18, 4)
  taxConfigSnapshot     Json?
  metadata              Json?
  createdAt             DateTime       @default(now())
  updatedAt             DateTime       @updatedAt
  document              FiscalDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@index([documentId, createdAt])
}

model FiscalParty {
  id             String         @id @default(uuid())
  documentId     String
  role           FiscalPartyRole
  name           String
  legalName      String?
  cnpjCpf        String
  stateRegistration String?
  municipalRegistration String?
  email          String?
  phone          String?
  address        Json?
  metadata       Json?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  document       FiscalDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@index([documentId, role])
  @@index([cnpjCpf])
}

model FiscalEmissionDraft {
  id                  String                @id @default(uuid())
  workspaceId         String
  workspaceBusinessId String?
  companyConfigId     String?
  status              FiscalDraftStatus     @default(DRAFT)
  documentType        FiscalDocumentType
  origin              FiscalDocumentOrigin
  saleId              String?
  stripeSessionId     String?
  stripePaymentIntentId String?
  emitAfterPayment    Boolean               @default(false)
  autoIssueEligible   Boolean               @default(false)
  payload             Json
  suggestion          Json?
  createdByUserId     String?
  issuedDocumentId    String?
  createdAt           DateTime              @default(now())
  updatedAt           DateTime              @updatedAt
  workspace           Workspace             @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyConfig       FiscalCompanyConfig?  @relation(fields: [companyConfigId], references: [id], onDelete: SetNull)

  @@index([workspaceId, workspaceBusinessId, status, createdAt])
  @@index([workspaceId, saleId, stripeSessionId])
}

model FiscalReceivedDocument {
  id                  String               @id @default(uuid())
  workspaceId         String
  workspaceBusinessId String?
  companyConfigId     String?
  type                FiscalReceivedType
  status              FiscalReceivedStatus @default(RECEIVED)
  manifestationStatus String?
  externalKey         String
  providerReference   String?
  focusReference      String?
  issuerName          String?
  issuerDocument      String?
  recipientDocument   String?
  amountTotal         Decimal?             @db.Decimal(18, 4)
  issuedAt            DateTime?
  receivedAt          DateTime?
  xmlUrl              String?
  pdfUrl              String?
  payload             Json?
  supplierId          String?
  costCenterId        String?
  categoryId          String?
  financialEntryId    String?
  purchaseId          String?
  mappedDocumentId    String?
  lastSyncAt          DateTime?
  lastError           String?
  metadata            Json?
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  workspace           Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyConfig       FiscalCompanyConfig? @relation(fields: [companyConfigId], references: [id], onDelete: SetNull)

  @@unique([workspaceId, type, externalKey])
  @@index([workspaceId, workspaceBusinessId, type, status, createdAt])
  @@index([workspaceId, manifestationStatus])
}

model FiscalSyncRun {
  id                String            @id @default(uuid())
  workspaceId       String
  workspaceBusinessId String?
  companyConfigId   String?
  syncType          FiscalSyncType
  trigger           FiscalSyncTrigger
  status            FiscalSyncStatus  @default(RUNNING)
  startedAt         DateTime          @default(now())
  finishedAt        DateTime?
  processedCount    Int               @default(0)
  createdCount      Int               @default(0)
  updatedCount      Int               @default(0)
  failedCount       Int               @default(0)
  requestSnapshot   Json?
  responseSnapshot  Json?
  lastError         String?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  workspace         Workspace         @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyConfig     FiscalCompanyConfig? @relation(fields: [companyConfigId], references: [id], onDelete: SetNull)

  @@index([workspaceId, workspaceBusinessId, syncType, status, startedAt])
}

model FiscalWebhookEvent {
  id                String              @id @default(uuid())
  workspaceId       String?
  source            FiscalWebhookSource
  providerEventId   String?
  eventType         String
  idempotencyKey    String              @unique
  status            FiscalWebhookStatus @default(RECEIVED)
  headers           Json?
  payload           Json
  signature         String?
  attempts          Int                 @default(0)
  processedAt       DateTime?
  lastError         String?
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  workspace         Workspace?          @relation(fields: [workspaceId], references: [id], onDelete: SetNull)

  @@index([source, eventType, createdAt])
  @@index([workspaceId, status, createdAt])
}

model FiscalIntegrationLog {
  id                String                    @id @default(uuid())
  workspaceId       String
  companyConfigId   String?
  documentId        String?
  system            FiscalWebhookSource
  direction         FiscalIntegrationDirection
  operation         String
  status            FiscalIntegrationStatus
  correlationId     String?
  httpStatus        Int?
  requestPayload    Json?
  responsePayload   Json?
  errorMessage      String?
  createdAt         DateTime                  @default(now())
  workspace         Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  companyConfig     FiscalCompanyConfig?      @relation(fields: [companyConfigId], references: [id], onDelete: SetNull)
  document          FiscalDocument?           @relation(fields: [documentId], references: [id], onDelete: SetNull)

  @@index([workspaceId, system, status, createdAt])
  @@index([documentId, createdAt])
}

model FiscalCatalogProfile {
  id                    String                @id @default(uuid())
  workspaceId           String
  workspaceBusinessId   String?
  itemType              FiscalCatalogItemType
  name                  String
  descriptionCommercial String?
  descriptionFiscal     String?
  sku                   String?
  unit                  String?
  defaultValue          Decimal?              @db.Decimal(18, 4)
  ncm                   String?
  serviceCode           String?
  cnae                  String?
  lcItem                String?
  cfopDefault           String?
  operationNature       String?
  taxConfig             Json?
  isActive              Boolean               @default(true)
  metadata              Json?
  createdByUserId       String?
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  workspace             Workspace             @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([workspaceId, workspaceBusinessId, itemType, isActive, createdAt])
  @@index([workspaceId, sku])
}

model FiscalOperationTemplate {
  id                  String             @id @default(uuid())
  workspaceId         String
  workspaceBusinessId String?
  name                String
  documentType        FiscalDocumentType
  itemType            FiscalCatalogItemType?
  serie               String?
  natureOperation     String?
  cfop                String?
  taxDefaults         Json?
  notes               String?
  isDefault           Boolean            @default(false)
  isActive            Boolean            @default(true)
  createdByUserId     String?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  workspace           Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([workspaceId, workspaceBusinessId, documentType, isActive, createdAt])
}

model WorkItemDocumentLink {
  id          String            @id @default(uuid())
  workspaceId String
  itemId      String
  documentId  String
  linkedBy    String
  createdAt   DateTime          @default(now())
  workspace   Workspace         @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  item        Item              @relation(fields: [itemId], references: [id], onDelete: Cascade)
  document    WorkspaceDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@unique([itemId, documentId])
  @@index([workspaceId, itemId, createdAt])
  @@index([workspaceId, documentId, createdAt])
}

model WorkspaceInvite {
  id               String         @id @default(uuid())
  workspaceId      String
  email            String
  role             MembershipRole @default(MEMBER)
  tokenHash        String         @unique
  invitedByUserId  String
  acceptedByUserId String?
  expiresAt        DateTime
  sentAt           DateTime       @default(now())
  acceptedAt       DateTime?
  revokedAt        DateTime?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  workspace        Workspace      @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  invitedBy        User           @relation("WorkspaceInviteSentBy", fields: [invitedByUserId], references: [id], onDelete: Cascade)
  acceptedBy       User?          @relation("WorkspaceInviteAcceptedBy", fields: [acceptedByUserId], references: [id], onDelete: SetNull)

  @@index([workspaceId, email])
  @@index([workspaceId, expiresAt, revokedAt, acceptedAt])
}

model WorkspaceMembership {
  id              String                    @id @default(uuid())
  workspaceId     String
  userId          String
  role            MembershipRole            @default(MEMBER)
  permissions     Json?
  createdAt       DateTime                  @default(now())
  workspace       Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  user            User                      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([workspaceId, userId])
}

model BoardTemplate {
  id              String                    @id @default(uuid())
  workspaceId     String
  name            String
  description     String?
  schema          Json
  rules           Json?
  createdAt       DateTime                  @default(now())
  workspace       Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  boards          Board[]
}

model Board {
  id              String                    @id @default(uuid())
  workspaceId     String
  templateId      String?
  name            String
  description     String?
  config          Json?
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  workspace       Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  template        BoardTemplate?            @relation(fields: [templateId], references: [id], onDelete: SetNull)
  columns         ColumnDefinition[]
  items           Item[]
}

model ColumnDefinition {
  id              String                    @id @default(uuid())
  boardId         String
  name            String
  code            String
  position        Int
  settings        Json?
  board           Board                     @relation(fields: [boardId], references: [id], onDelete: Cascade)

  @@unique([boardId, code])
}

model WorkItemType {
  id                  String              @id @default(uuid())
  workspaceId         String
  name                String
  slug                String
  description         String?
  color               String
  icon                String?
  position            Int
  isActive            Boolean             @default(true)
  usageRules          Json?
  acceptsParent       Boolean             @default(true)
  acceptsChecklist    Boolean             @default(true)
  acceptsDueDate      Boolean             @default(true)
  acceptsAssignee     Boolean             @default(true)
  acceptsTags         Boolean             @default(true)
  acceptsCustomFields Boolean             @default(true)
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt
  workspace           Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  items               Item[]
  customFieldScopes   CustomFieldScope[]
  fieldBindings       WorkItemFieldBinding[]

  @@unique([workspaceId, slug])
  @@index([workspaceId, isActive])
}

model WorkflowState {
  id            String               @id @default(uuid())
  workspaceId   String
  name          String
  slug          String
  category      String?
  color         String
  position      Int
  isActive      Boolean              @default(true)
  isTerminal    Boolean              @default(false)
  isEditable    Boolean              @default(true)
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt
  workspace     Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  items         Item[]
  columnMapping ColumnStateMapping[]

  @@unique([workspaceId, slug])
  @@index([workspaceId, isActive])
}

model BoardColumn {
  id            String               @id @default(uuid())
  workspaceId   String
  name          String
  slug          String
  position      Int
  wipLimit      Int?
  isActive      Boolean              @default(true)
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt
  workspace     Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  items         Item[]
  stateMappings ColumnStateMapping[]

  @@unique([workspaceId, slug])
  @@index([workspaceId, isActive])
}

model ColumnStateMapping {
  id          String       @id @default(uuid())
  workspaceId String
  columnId    String
  stateId     String
  position    Int          @default(0)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  workspace   Workspace    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  column      BoardColumn  @relation(fields: [columnId], references: [id], onDelete: Cascade)
  state       WorkflowState @relation(fields: [stateId], references: [id], onDelete: Cascade)

  @@unique([columnId, stateId])
  @@index([workspaceId, columnId])
  @@index([workspaceId, stateId])
}

model TagDefinition {
  id          String       @id @default(uuid())
  workspaceId String
  name        String
  slug        String
  color       String
  description String?
  isActive    Boolean      @default(true)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  workspace   Workspace    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  itemTags    WorkItemTag[]

  @@unique([workspaceId, slug])
  @@index([workspaceId, isActive])
}

model WorkspacePreferences {
  id                  String     @id @default(uuid())
  workspaceId         String     @unique
  defaultBoardMode    String     @default("dev")
  dateFormat          String     @default("dd/mm/yyyy")
  visibleCardFieldIds Json?
  settings            Json?
  createdAt           DateTime   @default(now())
  updatedAt           DateTime   @updatedAt
  workspace           Workspace  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
}

model CustomFieldDefinition {
  id            String              @id @default(uuid())
  workspaceId   String
  name          String
  slug          String
  description   String?
  variableKey   String?
  variableLabel String?
  variableDescription String?
  type          CustomFieldType
  isSystem      Boolean             @default(false)
  required      Boolean             @default(false)
  isEditable    Boolean             @default(true)
  isRemovable   Boolean             @default(true)
  position      Int
  isActive      Boolean             @default(true)
  settings      Json?
  defaultValue  Json?
  createdAt     DateTime            @default(now())
  updatedAt     DateTime            @updatedAt
  workspace     Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  options       CustomFieldOption[]
  values        CustomFieldValue[]
  scopes        CustomFieldScope[]
  bindings      WorkItemFieldBinding[]

  @@unique([workspaceId, slug])
  @@unique([workspaceId, variableKey])
  @@index([workspaceId, isActive])
}

model WorkItemFieldBinding {
  id                 String                @id @default(uuid())
  workspaceId        String
  typeId             String
  fieldId            String
  displayContext     String                @default("detail")
  position           Int                   @default(0)
  section            String?
  isVisible          Boolean               @default(true)
  isRequiredOverride Boolean?
  isReadonlyOverride Boolean?
  settings           Json?
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt
  workspace          Workspace             @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  type               WorkItemType          @relation(fields: [typeId], references: [id], onDelete: Cascade)
  field              CustomFieldDefinition @relation(fields: [fieldId], references: [id], onDelete: Cascade)

  @@unique([typeId, fieldId, displayContext])
  @@index([workspaceId, typeId, displayContext, position])
  @@index([workspaceId, fieldId, displayContext])
}

model CustomFieldOption {
  id          String                 @id @default(uuid())
  fieldId     String
  label       String
  value       String
  color       String?
  position    Int
  isActive    Boolean                @default(true)
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt
  field       CustomFieldDefinition  @relation(fields: [fieldId], references: [id], onDelete: Cascade)

  @@unique([fieldId, value])
  @@index([fieldId, position])
}

model CustomFieldScope {
  id        String                @id @default(uuid())
  fieldId   String
  typeId    String
  createdAt DateTime              @default(now())
  field     CustomFieldDefinition @relation(fields: [fieldId], references: [id], onDelete: Cascade)
  type      WorkItemType          @relation(fields: [typeId], references: [id], onDelete: Cascade)

  @@unique([fieldId, typeId])
}

model Item {
  id                String              @id @default(uuid())
  boardId           String
  workspaceId       String
  columnId          String?
  boardColumnId     String?
  type              String              @default("task")
  typeId            String?
  stateId           String?
  title             String
  description       String?
  status            String              @default("backlog")
  fields            Json?
  metadata          Json?
  checklist         Json?
  assigneeId        String?
  parentId          String?
  dueDate           DateTime?
  position          Int                 @default(0)
  createdBy         String
  updatedBy         String?
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  board             Board               @relation(fields: [boardId], references: [id], onDelete: Cascade)
  workspace         Workspace           @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  boardColumn       BoardColumn?        @relation(fields: [boardColumnId], references: [id], onDelete: SetNull)
  typeDefinition    WorkItemType?       @relation(fields: [typeId], references: [id], onDelete: SetNull)
  workflowState     WorkflowState?      @relation(fields: [stateId], references: [id], onDelete: SetNull)
  creator           User                @relation("ItemCreatedBy", fields: [createdBy], references: [id], onDelete: Restrict)
  assignee          User?               @relation("ItemAssignee", fields: [assigneeId], references: [id], onDelete: SetNull)
  updater           User?               @relation("ItemUpdatedBy", fields: [updatedBy], references: [id], onDelete: SetNull)
  parent            Item?               @relation("ItemParent", fields: [parentId], references: [id], onDelete: SetNull)
  children          Item[]              @relation("ItemParent")
  comments          ItemComment[]
  attachments       ItemAttachment[]
  history           ItemHistory[]
  searchDocuments   SearchDocument[]
  documentLinks     WorkItemDocumentLink[]
  customFieldValues CustomFieldValue[]
  tags              WorkItemTag[]
  viewPlacements    WorkItemViewPlacement[]
  aiAgentRuns       AIAgentRun[]
  automationApprovalRequests AutomationApprovalRequest[]
  communicationConversations CommunicationConversation[]
  communicationInteractions CommunicationInteraction[]

  @@index([workspaceId, typeId])
  @@index([workspaceId, stateId])
  @@index([workspaceId, boardColumnId])
  @@index([workspaceId, assigneeId])
  @@index([parentId])
}

model CustomFieldValue {
  id        String                @id @default(uuid())
  fieldId   String
  itemId    String
  value     Json
  updatedBy String?
  createdAt DateTime              @default(now())
  updatedAt DateTime              @updatedAt
  field     CustomFieldDefinition @relation(fields: [fieldId], references: [id], onDelete: Cascade)
  item      Item                  @relation(fields: [itemId], references: [id], onDelete: Cascade)
  updater   User?                 @relation("CustomFieldValueUpdatedBy", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([fieldId, itemId])
  @@index([itemId])
}

model WorkItemTag {
  id        String        @id @default(uuid())
  itemId    String
  tagId     String
  createdAt DateTime      @default(now())
  item      Item          @relation(fields: [itemId], references: [id], onDelete: Cascade)
  tag       TagDefinition @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([itemId, tagId])
  @@index([tagId])
}

model ItemComment {
  id              String                    @id @default(uuid())
  itemId          String
  authorId        String
  content         String
  metadata        Json?
  createdAt       DateTime                  @default(now())
  item            Item                      @relation(fields: [itemId], references: [id], onDelete: Cascade)
  author          User                      @relation(fields: [authorId], references: [id], onDelete: Restrict)
}

model ItemAttachment {
  id              String                    @id @default(uuid())
  itemId          String
  fileName        String
  url             String
  metadata        Json?
  createdAt       DateTime                  @default(now())
  item            Item                      @relation(fields: [itemId], references: [id], onDelete: Cascade)
}

model ItemHistory {
  id              String                    @id @default(uuid())
  itemId          String
  eventName       String
  payload         Json
  createdAt       DateTime                  @default(now())
  item            Item                      @relation(fields: [itemId], references: [id], onDelete: Cascade)
}

model AutomationWorkflow {
  id               String                      @id @default(uuid())
  workspaceId      String
  name             String
  description      String?
  status           String                      @default("draft")
  currentVersionId String?                     @unique
  createdById      String?
  createdAt        DateTime                    @default(now())
  updatedAt        DateTime                    @updatedAt
  workspace        Workspace                   @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  currentVersion   AutomationWorkflowVersion?  @relation("AutomationWorkflowCurrentVersion", fields: [currentVersionId], references: [id], onDelete: NoAction)
  createdBy        User?                       @relation("AutomationWorkflowCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  versions         AutomationWorkflowVersion[] @relation("AutomationWorkflowVersions")
  runs             AutomationRun[]

  @@index([workspaceId, status, updatedAt])
}

model AutomationWorkflowVersion {
  id                    String               @id @default(uuid())
  workflowId            String
  workspaceId           String
  version               Int
  status                String               @default("draft")
  definitionJson        Json
  graphNodesJson        Json
  graphEdgesJson        Json
  publishedAt           DateTime?
  publishedById         String?
  createdAt             DateTime             @default(now())
  workspace             Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  workflow              AutomationWorkflow    @relation("AutomationWorkflowVersions", fields: [workflowId], references: [id], onDelete: Cascade)
  currentForWorkflow    AutomationWorkflow?   @relation("AutomationWorkflowCurrentVersion")
  publishedBy           User?                 @relation("AutomationWorkflowVersionPublishedBy", fields: [publishedById], references: [id], onDelete: SetNull)
  runs                  AutomationRun[]

  @@unique([workflowId, version])
  @@index([workspaceId, status, createdAt])
  @@index([workflowId, status, version])
  @@index([workspaceId, publishedAt])
}

model AutomationRun {
  id                String                    @id @default(uuid())
  workspaceId       String
  workflowId        String
  workflowVersionId String
  triggerType       String
  triggerRefId      String?
  status            String                    @default("queued")
  contextJson       Json?
  startedAt         DateTime?
  finishedAt        DateTime?
  cancelledAt       DateTime?
  cancelReason      String?
  errorJson         Json?
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  workspace         Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  workflow          AutomationWorkflow        @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  workflowVersion   AutomationWorkflowVersion @relation(fields: [workflowVersionId], references: [id], onDelete: Restrict)
  stepRuns          AutomationStepRun[]
  scheduledSteps    AutomationScheduledStep[]
  events            AutomationRunEvent[]
  sideEffects       AutomationSideEffect[]
  approvalRequests  AutomationApprovalRequest[]
  communicationInteractions CommunicationInteraction[]

  @@index([workspaceId, status, createdAt])
  @@index([workflowId, createdAt])
  @@index([workflowVersionId, createdAt])
  @@index([workspaceId, triggerType, triggerRefId])
}

model AutomationStepRun {
  id              String                    @id @default(uuid())
  workspaceId     String
  runId           String
  nodeId          String
  nodeType        String
  status          String                    @default("queued")
  inputJson       Json?
  outputJson      Json?
  errorJson       Json?
  attempt         Int                       @default(1)
  idempotencyKey  String?
  startedAt       DateTime?
  finishedAt      DateTime?
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  workspace       Workspace                 @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  run             AutomationRun             @relation(fields: [runId], references: [id], onDelete: Cascade)
  scheduledSteps  AutomationScheduledStep[]
  events          AutomationRunEvent[]
  sideEffects     AutomationSideEffect[]
  approvalRequests AutomationApprovalRequest[]
  communicationInteractions CommunicationInteraction[]

  @@unique([runId, nodeId, attempt])
  @@unique([workspaceId, idempotencyKey])
  @@index([workspaceId, status, createdAt])
  @@index([runId, status, createdAt])
  @@index([runId, nodeId])
}

model AutomationScheduledStep {
  id            String            @id @default(uuid())
  workspaceId   String
  runId         String
  stepRunId     String
  nodeId        String
  purpose       String            @default("resume")
  executeAt     DateTime
  status        String            @default("scheduled")
  lockedAt      DateTime?
  lockedBy      String?
  cancelledAt   DateTime?
  cancelReason  String?
  attempts      Int               @default(0)
  idempotencyKey String?
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
  workspace     Workspace         @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  run           AutomationRun     @relation(fields: [runId], references: [id], onDelete: Cascade)
  stepRun       AutomationStepRun @relation(fields: [stepRunId], references: [id], onDelete: Cascade)

  @@unique([workspaceId, idempotencyKey])
  @@index([workspaceId, status, executeAt])
  @@index([workspaceId, purpose, status, executeAt])
  @@index([status, executeAt])
  @@index([runId, status])
  @@index([stepRunId])
}

model AutomationRunEvent {
  id          String             @id @default(uuid())
  workspaceId String
  runId       String
  stepRunId   String?
  eventType   String
  level       String             @default("info")
  message     String
  payloadJson Json?
  createdAt   DateTime           @default(now())
  workspace   Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  run         AutomationRun      @relation(fields: [runId], references: [id], onDelete: Cascade)
  stepRun     AutomationStepRun? @relation(fields: [stepRunId], references: [id], onDelete: SetNull)

  @@index([workspaceId, runId, createdAt])
  @@index([runId, eventType, createdAt])
  @@index([stepRunId, createdAt])
  @@index([workspaceId, eventType, createdAt])
}

model AutomationSideEffect {
  id             String             @id @default(uuid())
  workspaceId    String
  runId          String
  stepRunId      String
  sideEffectType String
  channel        String?
  provider       String?
  status         String             @default("queued")
  idempotencyKey String
  payloadJson    Json
  resultJson     Json?
  errorJson      Json?
  templateVersionId String?
  contactId      String?
  contactChannelId String?
  approvalRequestId String?
  conversationId String?
  attempts       Int                @default(0)
  maxAttempts    Int                @default(3)
  nextAttemptAt  DateTime           @default(now())
  lockedAt       DateTime?
  lockedBy       String?
  cancelledAt    DateTime?
  cancelReason   String?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  processedAt    DateTime?
  workspace      Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  run            AutomationRun      @relation(fields: [runId], references: [id], onDelete: Cascade)
  stepRun        AutomationStepRun  @relation(fields: [stepRunId], references: [id], onDelete: Cascade)
  templateVersion CommunicationTemplateVersion? @relation(fields: [templateVersionId], references: [id], onDelete: SetNull)
  providerEvents CommunicationProviderEvent[]
  contact        CommunicationContact? @relation(fields: [contactId], references: [id], onDelete: SetNull)
  contactChannel CommunicationContactChannel? @relation(fields: [contactChannelId], references: [id], onDelete: SetNull)
  approvalRequest AutomationApprovalRequest? @relation(fields: [approvalRequestId], references: [id], onDelete: SetNull)
  conversation    CommunicationConversation? @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  interactions   CommunicationInteraction[]

  @@unique([workspaceId, idempotencyKey])
  @@index([workspaceId, status, nextAttemptAt])
  @@index([status, nextAttemptAt])
  @@index([runId, status])
  @@index([stepRunId])
  @@index([templateVersionId])
  @@index([contactId])
  @@index([contactChannelId])
  @@index([approvalRequestId])
  @@index([conversationId])
}

model AutomationApprovalRequest {
  id            String             @id @default(uuid())
  workspaceId   String
  runId         String
  stepRunId     String
  contactId     String?
  workItemId    String?
  conversationId String?
  type          String
  status        String             @default("pending")
  title         String
  description   String?
  payloadJson   Json?
  decisionJson  Json?
  requestedBy   String?
  reviewedBy    String?
  requestedAt   DateTime           @default(now())
  reviewedAt    DateTime?
  expiresAt     DateTime?
  createdAt     DateTime           @default(now())
  updatedAt     DateTime           @updatedAt
  workspace     Workspace          @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  run           AutomationRun      @relation(fields: [runId], references: [id], onDelete: Cascade)
  stepRun       AutomationStepRun  @relation(fields: [stepRunId], references: [id], onDelete: Cascade)
  contact       CommunicationContact? @relation(fields: [contactId], references: [id], onDelete: SetNull)
  workItem      Item?              @relation(fields: [workItemId], references: [id], onDelete: SetNull)
  conversation  CommunicationConversation? @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  sideEffects   AutomationSideEffect[]
  interactions  CommunicationInteraction[]

  @@index([workspaceId, status, requestedAt])
  @@index([workspaceId, runId, status])
  @@index([stepRunId])
  @@index([contactId])
  @@index([workItemId])
  @@index([conversationId])
}

model CommunicationContact {
  id               String                       @id @default(uuid())
  workspaceId      String
  displayName      String?
  firstName        String?
  lastName         String?
  companyName      String?
  sourceType       String                       @default("manual")
  sourceId         String?
  primaryEmail     String?
  primaryPhone     String?
  preferredChannel String?
  status           String                       @default("active")
  metadataJson     Json?
  createdAt        DateTime                     @default(now())
  updatedAt        DateTime                     @updatedAt
  archivedAt       DateTime?
  workspace        Workspace                    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  channels         CommunicationContactChannel[]
  sideEffects      AutomationSideEffect[]
  interactions     CommunicationInteraction[]
  conversations    CommunicationConversation[]
  conversationWindows CommunicationConversationWindow[]
  providerEvents   CommunicationProviderEvent[]
  approvalRequests AutomationApprovalRequest[]

  @@index([workspaceId, status, updatedAt])
  @@index([workspaceId, sourceType, sourceId])
  @@index([workspaceId, primaryEmail])
  @@index([workspaceId, primaryPhone])
}

model CommunicationContactChannel {
  id                String                     @id @default(uuid())
  workspaceId       String
  contactId         String
  channel           String
  address           String
  normalizedAddress String
  label             String?
  isPrimary         Boolean                    @default(false)
  status            String                     @default("active")
  verifiedAt        DateTime?
  lastUsedAt        DateTime?
  metadataJson      Json?
  createdAt         DateTime                   @default(now())
  updatedAt         DateTime                   @updatedAt
  workspace         Workspace                  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  contact           CommunicationContact       @relation(fields: [contactId], references: [id], onDelete: Cascade)
  sideEffects       AutomationSideEffect[]
  interactions      CommunicationInteraction[]
  conversations     CommunicationConversation[]
  conversationWindows CommunicationConversationWindow[]
  providerEvents    CommunicationProviderEvent[]

  @@unique([workspaceId, channel, normalizedAddress])
  @@index([workspaceId, contactId, channel])
  @@index([workspaceId, channel, status])
}

model CommunicationConversation {
  id                 String                       @id @default(uuid())
  workspaceId        String
  contactId          String
  contactChannelId   String?
  primaryChannel     String
  status             String                       @default("open")
  priority           String                       @default("normal")
  assignedToId       String?
  workItemId         String?
  lastMessageAt      DateTime?
  lastInboundAt      DateTime?
  lastOutboundAt     DateTime?
  lastMessagePreview String?
  unreadCount        Int                          @default(0)
  metadataJson       Json?
  createdAt          DateTime                     @default(now())
  updatedAt          DateTime                     @updatedAt
  archivedAt         DateTime?
  resolvedAt         DateTime?
  workspace          Workspace                    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  contact            CommunicationContact         @relation(fields: [contactId], references: [id], onDelete: Cascade)
  contactChannel     CommunicationContactChannel? @relation(fields: [contactChannelId], references: [id], onDelete: SetNull)
  assignedTo         User?                        @relation("CommunicationConversationAssignedTo", fields: [assignedToId], references: [id], onDelete: SetNull)
  workItem           Item?                        @relation(fields: [workItemId], references: [id], onDelete: SetNull)
  interactions       CommunicationInteraction[]
  sideEffects        AutomationSideEffect[]
  approvalRequests   AutomationApprovalRequest[]

  @@index([workspaceId, status, lastMessageAt])
  @@index([workspaceId, contactId, primaryChannel, status])
  @@index([workspaceId, contactChannelId, status])
  @@index([workspaceId, workItemId, status])
  @@index([workspaceId, assignedToId, status])
  @@index([workspaceId, lastInboundAt])
}

model CommunicationInteraction {
  id                String                       @id @default(uuid())
  workspaceId       String
  conversationId    String?
  contactId         String?
  contactChannelId  String?
  sideEffectId      String?
  providerEventId   String?
  approvalRequestId String?
  runId             String?
  stepRunId         String?
  workItemId        String?
  direction         String
  channel           String
  type              String
  status            String?
  provider          String?
  textPreview       String?
  bodyJson          Json?
  occurredAt        DateTime                     @default(now())
  metadataJson      Json?
  createdAt         DateTime                     @default(now())
  workspace         Workspace                    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  conversation      CommunicationConversation?   @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  contact           CommunicationContact?        @relation(fields: [contactId], references: [id], onDelete: SetNull)
  contactChannel    CommunicationContactChannel? @relation(fields: [contactChannelId], references: [id], onDelete: SetNull)
  sideEffect         AutomationSideEffect?        @relation(fields: [sideEffectId], references: [id], onDelete: SetNull)
  providerEvent      CommunicationProviderEvent?  @relation(fields: [providerEventId], references: [id], onDelete: SetNull)
  approvalRequest    AutomationApprovalRequest?   @relation(fields: [approvalRequestId], references: [id], onDelete: SetNull)
  run                AutomationRun?               @relation(fields: [runId], references: [id], onDelete: SetNull)
  stepRun            AutomationStepRun?           @relation(fields: [stepRunId], references: [id], onDelete: SetNull)
  workItem           Item?                        @relation(fields: [workItemId], references: [id], onDelete: SetNull)

  @@index([workspaceId, contactId, occurredAt])
  @@index([workspaceId, conversationId, occurredAt])
  @@index([workspaceId, sideEffectId])
  @@index([workspaceId, providerEventId])
  @@index([workspaceId, approvalRequestId])
  @@index([workspaceId, runId])
  @@index([workspaceId, workItemId])
}

model CommunicationTemplate {
  id          String                         @id @default(uuid())
  workspaceId String
  name        String
  key         String
  channel     String
  category    String                        @default("follow_up")
  status      String                        @default("draft")
  description String?
  providerTemplateName String?
  providerTemplateId String?
  language    String?
  approvalStatus String                     @default("draft")
  createdById String?
  createdAt   DateTime                      @default(now())
  updatedAt   DateTime                      @updatedAt
  archivedAt  DateTime?
  workspace   Workspace                     @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  createdBy   User?                         @relation("CommunicationTemplateCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  versions    CommunicationTemplateVersion[]

  @@unique([workspaceId, key])
  @@index([workspaceId, channel, status, updatedAt])
  @@index([workspaceId, category, status])
}

model CommunicationTemplateVersion {
  id            String                     @id @default(uuid())
  workspaceId   String
  templateId    String
  version       Int
  status        String                     @default("draft")
  approvalStatus String                    @default("draft")
  providerTemplateName String?
  providerTemplateId String?
  language      String?
  componentsJson Json?
  subject       String?
  textBody      String?
  htmlBody      String?
  variablesJson Json?
  metadataJson  Json?
  publishedAt   DateTime?
  publishedById String?
  createdAt     DateTime                   @default(now())
  updatedAt     DateTime                   @updatedAt
  workspace     Workspace                  @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  template      CommunicationTemplate      @relation(fields: [templateId], references: [id], onDelete: Cascade)
  publishedBy   User?                      @relation("CommunicationTemplateVersionPublishedBy", fields: [publishedById], references: [id], onDelete: SetNull)
  sideEffects   AutomationSideEffect[]

  @@unique([templateId, version])
  @@index([workspaceId, status, createdAt])
  @@index([workspaceId, approvalStatus, updatedAt])
  @@index([workspaceId, templateId, status, version])
}

model CommunicationConversationWindow {
  id          String    @id @default(uuid())
  workspaceId String
  contactId   String
  contactChannelId String?
  channel     String
  provider    String?
  openedAt    DateTime
  expiresAt   DateTime
  lastInboundAt DateTime?
  lastOutboundAt DateTime?
  source      String
  status      String    @default("open")
  metadataJson Json?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  contact     CommunicationContact @relation(fields: [contactId], references: [id], onDelete: Cascade)
  contactChannel CommunicationContactChannel? @relation(fields: [contactChannelId], references: [id], onDelete: SetNull)

  @@index([workspaceId, contactId, channel, status])
  @@index([workspaceId, contactChannelId, channel, status])
  @@index([workspaceId, channel, expiresAt])
}

model ContactConsent {
  id          String    @id @default(uuid())
  workspaceId String
  contactType String?
  contactId   String?
  channel     String
  address     String
  status      String    @default("unknown")
  source      String?
  reason      String?
  optInAt     DateTime?
  optOutAt    DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@unique([workspaceId, channel, address])
  @@index([workspaceId, channel, status, updatedAt])
  @@index([workspaceId, contactType, contactId])
}

model CommunicationSuppression {
  id          String    @id @default(uuid())
  workspaceId String
  channel     String
  address     String
  reason      String
  source      String?
  createdAt   DateTime  @default(now())
  expiresAt   DateTime?
  metadataJson Json?
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@unique([workspaceId, channel, address])
  @@index([workspaceId, channel, createdAt])
  @@index([workspaceId, channel, reason])
}

model CommunicationUnsubscribeToken {
  id          String    @id @default(uuid())
  workspaceId String
  channel     String
  address     String
  tokenHash   String    @unique
  category    String?
  expiresAt   DateTime
  usedAt      DateTime?
  createdAt   DateTime  @default(now())
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([workspaceId, channel, address])
  @@index([expiresAt, usedAt])
}

model CommunicationProviderEvent {
  id                String                       @id @default(uuid())
  workspaceId       String?
  provider          String
  channel           String
  providerEventId   String
  providerMessageId String?
  eventType         String
  status            String                       @default("received")
  sideEffectId      String?
  contactId         String?
  contactChannelId  String?
  payloadJson       Json?
  normalizedJson    Json?
  errorJson         Json?
  receivedAt        DateTime                     @default(now())
  processedAt       DateTime?
  createdAt         DateTime                     @default(now())
  updatedAt         DateTime                     @updatedAt
  workspace         Workspace?                   @relation(fields: [workspaceId], references: [id], onDelete: SetNull)
  sideEffect        AutomationSideEffect?        @relation(fields: [sideEffectId], references: [id], onDelete: SetNull)
  contact           CommunicationContact?        @relation(fields: [contactId], references: [id], onDelete: SetNull)
  contactChannel    CommunicationContactChannel? @relation(fields: [contactChannelId], references: [id], onDelete: SetNull)
  interactions      CommunicationInteraction[]

  @@unique([provider, providerEventId])
  @@index([workspaceId, receivedAt])
  @@index([provider, channel, providerMessageId])
  @@index([sideEffectId])
  @@index([contactId])
  @@index([contactChannelId])
  @@index([eventType, receivedAt])
  @@index([status, receivedAt])
}

model AutomationView {
  id          String                 @id @default(uuid())
  workspaceId String
  key         String
  name        String
  description String?
  position    Int                    @default(0)
  isSystem    Boolean                @default(false)
  isActive    Boolean                @default(true)
  settings    Json?
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt
  workspace   Workspace              @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  columns     AutomationViewColumn[]
  placements  WorkItemViewPlacement[]

  @@unique([workspaceId, key])
  @@index([workspaceId, isActive, position])
}

model AutomationViewColumn {
  id          String               @id @default(uuid())
  workspaceId String
  viewId      String
  key         String
  name        String
  description String?
  color       String
  position    Int                  @default(0)
  isActive    Boolean              @default(true)
  isTerminal  Boolean              @default(false)
  settings    Json?
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt
  workspace   Workspace            @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  view        AutomationView       @relation(fields: [viewId], references: [id], onDelete: Cascade)
  placements  WorkItemViewPlacement[]

  @@unique([viewId, key])
  @@index([workspaceId, viewId, isActive, position])
}

model WorkItemViewPlacement {
  id          String                @id @default(uuid())
  workspaceId String
  itemId      String
  viewId      String
  columnId    String
  position    Int                   @default(0)
  metadata    Json?
  updatedBy   String?
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt
  workspace   Workspace             @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  item        Item                  @relation(fields: [itemId], references: [id], onDelete: Cascade)
  view        AutomationView        @relation(fields: [viewId], references: [id], onDelete: Cascade)
  column      AutomationViewColumn  @relation(fields: [columnId], references: [id], onDelete: Cascade)
  updater     User?                 @relation("WorkItemViewPlacementUpdatedBy", fields: [updatedBy], references: [id], onDelete: SetNull)

  @@unique([itemId, viewId])
  @@index([workspaceId, viewId, columnId, position])
}

model SearchDocument {
  id              String                    @id @default(uuid())
  itemId          String
  workspaceId     String
  boardId         String
  content         String
  embedding       Float[]                  @default([])
  metadata        Json?
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  item            Item                      @relation(fields: [itemId], references: [id], onDelete: Cascade)

  @@index([workspaceId, boardId])
}

model AIAgent {
  id            String       @id @default(uuid())
  workspaceId   String
  key           String
  name          String
  description   String?
  model         String
  temperature   Float        @default(0.2)
  systemPrompt  String
  config        Json?
  isActive      Boolean      @default(true)
  isDefault     Boolean      @default(false)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
  workspace     Workspace    @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  runs          AIAgentRun[]

  @@unique([workspaceId, key])
  @@index([workspaceId, isActive, updatedAt])
}

model AIAgentRun {
  id            String    @id @default(uuid())
  workspaceId   String
  agentId       String
  itemId        String?
  requestedBy   String
  status        String
  input         Json
  output        Json?
  error         String?
  provider      String?
  model         String?
  latencyMs     Int?
  inputTokens   Int?
  outputTokens  Int?
  totalTokens   Int?
  estimatedCostUsd Float?
  startedAt     DateTime?
  finishedAt    DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  workspace     Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)
  agent         AIAgent   @relation(fields: [agentId], references: [id], onDelete: Cascade)
  item          Item?     @relation(fields: [itemId], references: [id], onDelete: SetNull)

  @@index([workspaceId, createdAt])
  @@index([agentId, createdAt])
  @@index([itemId, createdAt])
}

model DomainOutbox {
  id              String                    @id @default(uuid())
  eventName       String
  aggregateType   String
  aggregateId     String
  payload         Json
  occurredAt      DateTime
  processedAt     DateTime?
  retries         Int                       @default(0)
  nextAttemptAt   DateTime                  @default(now())
  deadLetteredAt  DateTime?
  lastError       String?
  createdAt       DateTime                  @default(now())

  @@index([processedAt, deadLetteredAt, nextAttemptAt])
}

model AuditEvent {
  id              String                    @id @default(uuid())
  eventName       String
  severity        AuditSeverity             @default(INFO)
  actorId         String?
  workspaceId     String?
  metadata        Json?
  happenedAt      DateTime                  @default(now())
}

model TelemetryEvent {
  id          String   @id
  category    String
  eventName   String
  success     Boolean?
  userId      String?
  workspaceId String?
  method      String?
  route       String?
  statusCode  Int?
  durationMs  Int?
  durationNs  BigInt?
  reason      String?
  provider    String?
  ipHash      String?
  country     String?
  city        String?
  userAgent   String?
  browser     String?
  os          String?
  deviceType  String?
  metadata    Json?
  occurredAt  DateTime @default(now())

  @@index([category, eventName, occurredAt])
  @@index([userId, occurredAt])
  @@index([workspaceId, occurredAt])
  @@index([route, occurredAt])
}
